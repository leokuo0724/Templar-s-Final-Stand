export enum CardType {
  TEMPLAR,
  ENEMY,
  WEAPON,
}

export enum Belongs {
  PLAYER,
  ENEMY,
}
export enum AttackDirection {
  FRONT, // 1 grid
  FORWARD, // 2 grid
  AROUND, // 4 grid
  LINE, // 4 grid
}
